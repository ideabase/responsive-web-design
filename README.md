# RWD Kent Course Website
> Course information, schedules, syllabi, and more

## Uses git lfs for .jpg, .png, .zip, .ai, .psd, etc.
- run the following in terminal to pull larger files from the repository
```sh
git lfs install
git lfs fetch
git lfs pull
```
